# Initialization of variables
number = 10
count = 0
flag = 'yes'
message = 'looping'

# Main while loop
while count < number:
    # Nested while loop
    while flag == 'yes':
        if count < number / 2:
            product = count * 2
            sum_result = count + product
            flag = 'no'
        else:
            difference = number - count
            quotient = number / (count + 1)
            flag = 'maybe'
        print(flag)

        # Inner while loop
        inner_count = 0
        while inner_count < 3:
            if product > 10:
                message = 'greater'
                inner_count = inner_count + 1
            else:
                message = 'smaller'
                inner_count = inner_count + 1

            # Another inner while loop
            sub_count = 0
            while sub_count < 2:
                if sum_result < 15:
                    final_message = 'less than 15'
                else:
                    final_message = 'more than 15'
                sub_count = sub_count + 1
        count = count + 1
    # End of nested while loop
    count = count + 1
# End of main while loop
print(final_message)